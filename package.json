{
  "name": "datastraw-monorepo-scripts",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "bash -lc \"set -a && . backend/.env && set +a && . .venv/bin/activate 2>/dev/null || true && python -m uvicorn app.main:app --host 0.0.0.0 --port 8000\"",
    "dev": "bash scripts/dev.sh"
  }
}
